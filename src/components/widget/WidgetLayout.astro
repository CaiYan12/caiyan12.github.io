---
interface Props {
	title: string;
	icon?: string;
}

const { title, icon } = Astro.props;
---

<section class="widget">
	{icon && (
		<span class="icon">
			<i class={`fa ${icon}`} aria-hidden="true"></i>
		</span>
	)}
	<h3>{title}</h3>
	<slot />
</section>
