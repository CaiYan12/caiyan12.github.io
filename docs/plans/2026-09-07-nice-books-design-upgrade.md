# Nice Books 私人藏书桌设计升级

状态：实施及本地build4验收完成，交付人工审查。用户已批准方案；2026-09-07 追加要求使用 gpt-5.6-luna / high 并行修复、独立检验，直接交付人工审查。保留工作区，不提交、不推送、不部署。

## 目标与全局约束

视觉 / 立体质感 / 动画交互三维度升级。保留暖纸、墨色、印章红、书宋标题和手写荐语，允许重做原 handoff 的色值、排版、厚度和动效限制。独立壳不加载主站 Layout/Pio/播放器；Book 数据与路由、访问级随机、六字段搜索、Pagefind 排除、SSR/客户端 render 单源保持。Astro/Tailwind 不升级。全部中文 UTF-8。保留已有修改。GSAP 限定 books 主书动效。没有执行的验收不得写通过。

## 阶段 1：基础设计系统
- [x] 接通 font-nb-body；正文思源黑体、标题书宋、短荐语/摘抄手写、编号等宽。
- [x] 小字对比度 >=4.5:1；正文16px、辅助信息>=13px；统一 --nb-* token，保持 nb. Tailwind 命名空间与互斥状态色。
- [x] 修复列表 group hover；保留 [hidden] 语义；搜索可见标签、44px触摸目标、焦点与错误状态。
- [x] check / 单测 / build；浏览器核对字体与状态。

## 阶段 2：书封和实体结构
- [x] 文学、历史人文、科幻科普、散文随笔四套原创确定性 SVG；独立 ID 展示映射，未知 ID 回退文学。
- [x] 封面最大、页块内缩、书口只右侧露出、左侧装订/书脊；压痕高光、接触阴影；不做底部纸垫。
- [x] 外层允许厚度/胶带外伸，内层裁切图像；主书/网格/列表三档；无WebGL。
- [x] featured有腰封、普通裸封；腰封正面与侧折返；文案只用站长推荐和现有荐语最多两行；书名不遮挡；列表不显示腰封文字。
- [x] 保留图片入口与失败生成回退，不裁切主体；Book与data.json业务契约不变。
- [x] 覆盖22书/长标题/各变体/回退的单测及构建。

## 阶段 3：三页视觉
- [x] 首页主书视觉中心、减少套卡、书与荐语双栏；推荐六本缩小封面形成陈列节奏。
- [x] 书库标题→搜索视图→标签→结果；保持快速检索。
- [x] 详情桌面双栏，移动书名作者在封面前、封面适当缩小、荐语标题去重。
- [x] 网格<360单列、360–759两列、>=760三列；同架>=1080四列。
- [x] 320/390/540/541/760/960/1280、200%缩放检查；smoke/build。

## 阶段 4：可靠动效
- [x] 首次书本/书名/荐语编排<=600ms；主书换书约420ms（旧书退出/新书落桌），无额外模拟延时。
- [x] CSS常规反馈；GSAP仅主书多层时间线，scoped context及销毁清理。
- [x] 双今日入口共用忙碌状态、同时禁用、换书中…、成功/异常恢复；推荐组独立忙碌与交接。
- [x] 搜索输入不反复动画；标签/视图160–200ms可接管；hover上提4px/倾斜<=3度、仅fine hover；按下100–160ms。
- [x] Swup保持main协议，before-swap取消动画/计时器/请求，查询限定main；书库失败显示重试并解除错误初始化守卫。
- [x] reduced motion取消位移/倾斜/stagger delay，保留短透明度/颜色；无漂浮/滚动劫持/全屏模糊/逐帧阴影。
- [x] 忙碌/异常/连击/中断/请求重试/减少动效/逐帧时序测试，单测smoke build。

## 阶段 5：交付
- [x] build+preview三页实机，桌面/手机截图和动作连续帧。
- [x] 直接访问、Swup、后退、重复进入、搜索标签、载入更多、字体/图片失败、键盘。
- [x] 主站资源隔离、体积及性能采样，保存真实结果。
- [x] 更新设计说明、README TODO、AGENTS可修改部分；固定规则不动。
- [x] 独立review、修复回归、直接交付人工审查。

## 执行分工

1. cover-system：src/nice-books/lib（cover/render与新增展示配置/测试）、styles/books.css。
2. page-layout：src/pages/books三页、src/nice-books/components、tailwind.config.cjs。
3. motion：src/nice-books/scripts、package.json/pnpm-lock.yaml、scripts/nice-books-smoke.mjs。
4. qa：新增 scripts/nice-books-design-qa.mjs（验证和截图），不改以上实现文件。

各分工不得互相编辑所有权文件；接口经消息确认。并行实现来自用户明确要求，覆盖SDD默认串行限制。各任务完成后控制器保存diff供独立审查；不自动提交。
