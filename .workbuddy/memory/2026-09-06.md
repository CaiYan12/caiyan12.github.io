# 2026-09-06

## Nice Books V1 移植完成（caiyan12.github.io /books/）

- 从 `D:\pages\test\nice-books\prototype`（Design Handoff）移植为生产模块：`src/nice-books/`（types/data/lib/render/scripts/styles）+ `src/pages/books/`（index/archive/[id] 三路由，独立壳仿 ai-news）。
- 关键决策（Grilling 8 项，用户裁定）：独立壳 / TS 数据文件 / 书库客户端六字段过滤且全部 books 页 `data-pagefind-ignore="all"`（博客全局搜索只搜文章）/ SVG 书封兜底 / navBarConfig.resourceSite 加「每日好书」(noSwup) / books 内部启用 swup / Tailwind 重写（`nb.` 色板命名空间防与 ai-news `--t-*` 键冲突）/ SSR 固定书兜底+客户端随机替换。
- 架构要点：`lib/render.ts` 字符串构造器单源 SSR/客户端标记（换一换 innerHTML 无法走 Astro 组件）；swup 协议 = 顶层 init + astro:page-load 重跑 + main dataset 守卫 + 元素缺失早退；`@swup/astro` 默认 loadOnIdle（window.swup 空闲后才有，测试需等待）；**Tailwind utility display 覆盖 `[hidden]` 属性**——books.css 补 `[hidden]{display:none!important}`（坑，勿删）。
- 测试：`pnpm test:nice-books`（node --test 27 例，mulberry32 注入 RNG）串入 build 链头部；`pnpm smoke:nice-books`（Playwright 43 项实机：随机契约/loading/去重/书库六字段/直达/404/swup 切页后退/重复绑定防护），三连跑稳定。
- 踩坑记录：① `[id].astro` 未实现时 swup hover 预载 /books/:id/ 触发 dev 500 → Astro dev 向 HMR 广播错误，所有已开页面弹 vite-error-overlay 拦截点击（表现为随机位置点击超时）；② 本机 cdn.jsdelivr.net TLS 握手失败（about.astro 同渠道既有情况，字体栈有系统兜底，烟测对外部 CDN 失败单独统计不计 FAIL）；③ result-line 文本带「（显示前 12 本）」后缀、级联首卡 delay=0s 属正确值，断言勿写成精确匹配。
- 文档同步：README 命令、CHANGELOG 新条目、AGENTS.md 新架构节、CONTEXT.md 四术语（Nice Books/今日好书/书库/站长荐语）。
- 视觉对比截图（1280+390 × 3 页 × 生产/原型）：`output/nice-books-visual/`（已 gitignore）。
- 追加：封面升级立体书本样式（commit 8984ae6）——`nb-book3d` 三层（背板书壳 #453c31 / 书页纸张条纹层 / 前封面），`--p:9px --e:2px`，列表行 `nb-book3d-thin`（3px/1px）；只改 books.css + render.ts coverHTML。教训：① Playwright 元素截图会裁掉 transform 溢出，截立体层要用父容器或 boundingBox 扩边 clip；② jsDelivr 挂起请求会让 `waitUntil:'networkidle'` 永不达成（本地验证一律 domcontentloaded+waitForSelector）；③ 截图脚本选择器必须用生产类名（#nb-hero .nb-book3d），原型类名（hero-cover-wrap/detail-cover-col）在 Tailwind 版里不存在。
- 二次修正（commit 431a39d）：初版做成右+下双侧突出（像纸垫）。用户指出传统书本结构：**封面是最大硬壳包住纸张上/下/左三侧，页纹只从右侧书口露出，且纸张比封面小**。终版层级：封面(齐平) > 背板壳(上下内缩 3px) > 纸张页纹(上下内缩 6px)，右侧依次露出 页纹 9px + 背板边 2px。经验：做"实体物品的 2.5D"时先确认各层的物理包含关系与相对大小，别默认各层同尺寸位移。
- 书库空数据事故（commit f8bfa52，用户报告）：三个叠加缺陷——① **swup 替换 main 会丢失 DOM 内嵌 JSON script**（实测 jsonLen=0，数据改走静态端点 /books/data.json）；② **SwupScriptsPlugin 不会执行 swup 导航后新页的 module 脚本**（实测 swup 后 DOM 里还是首页脚本）→ 三页共享 scripts/main.ts 统一入口（init 自查元素早退 + astro:page-load 统一重跑，即主站 theme-script 的「唯一协议+多 init」模式）；③ initHome/initArchive 守卫标记打在元素确认前且两页 main 同 ID（#nb-books-main）→ swup 后旧页监听器误标记封锁对方 init → 标记移到元素确认后。附带：replaceState(null) 会抹掉 swup history state 断裂 popstate → 必须 replaceState(history.state, ...)；搜索框补按钮+回车。**通用教训：swup 站点的新页逻辑绝不能依赖新页脚本的自动执行，必须全局入口 + astro:page-load 调度**。烟测增至 46 项。
- 选中药丸空白（commit 348570b，用户报告）：tagPillHTML 基础类 bg-transparent + 选中态 bg-nb-ink 同 specificity 冲突，胜负由 Tailwind 产物 CSS 源顺序决定（与标记书写顺序无关）→ 墨底失效、反白字落米纸底=空白。修复：状态色（bg/text/border）改为未选中/选中两套互斥拼接。**通用教训：同一元素的同一 CSS 属性组 utility 永远互斥拼接，不要"基础类+状态类"叠加**。烟测增至 47 项。
- 页眉高亮失效（commit 24d7dcf，用户报告）：SiteHeader 基础类 border-transparent/text-nb-ink-soft + 当前态 border-nb-seal/text-nb-ink——**与 tagPill 完全相同的第三起冲突模式**（border-transparent 排后胜出）。修复同款互斥拼接。**此模式已在 books 出现三次（tagPill/vt-btn/SiteHeader），任何"基础色类+条件色类"写法都是隐患**。烟测增至 48 项。
- 高亮不随 swup 迁移（commit 7ca969b，用户报告）：页眉在 swup 容器外不随切页替换，SSR 高亮停留旧页。修复：shared.ts 新增 `syncHeaderNav()`（pathname 判定，详情页归入书库分支；全量 className 替换互斥色类），main.ts init 周期调用——即主站 syncNavHighlight 的 books 版。SSR 侧 SiteHeader 标记类改引 shared 常量（NAV_BASE/NAV_ACTIVE/NAV_IDLE 单一来源）+ 链接加 data-nb-nav 锚点。烟测增至 49 项。
- 切页动画重设计（commit bdc7e02 + 5cba51e 修正，用户需求 @animate）：原 0.3s body 淡入淡出被提前打断形同虚设（opacity 时间线实测 45ms 即回弹——子元素 transitionend 冒泡/动画等待竞态）。重设计：动画目标 body→**main 容器**（页眉页脚静止）；两段式 is-animating 离场（opacity→0+Y-8px, 170ms ease-in）/ 类移除时回落（240ms ease-out 自下方上浮）。**踩坑：初版加 `html.is-rendering { opacity:1 }` 入场规则导致新页面直接出现无动画——swup 添加 is-rendering 先于容器替换，新 main 插入时即命中终态，过渡无从发生；正确做法是 swup 官方 fade 模式（只有 is-animating 隐藏规则，入场=类全部移除瞬间的回落过渡）**。**经验：① swup 切页动画作用于 main 容器而非 body（语义正确且页眉稳定）；② 验证 swup 动画时采样闭包不能缓存容器引用（swup 会整体替换 main 元素，必须每帧重查）；③ 本地 dev 响应快会让旧 0.3s 动画"看似存在实则被打断"，时间线采样才能暴露；④ swup 的 is-rendering 类添加早于 DOM 替换，任何依赖其"入场初态"的编排都会失效**。
- intro「换一换」按钮化（commit a3e2cdc，用户需求）：首页宣言里的「换一换」span 升级为真 button（#nb-intro-shuffle），hover 转印章红、点击触发 shuffleHero；shuffleHero 加 swapping 闭包守卫，换书动画期间主按钮/intro 按钮的重复触发均被忽略（无图标故不加 240ms 模拟 loading，直接进动画）。
- 下划线掉行修复（commit aa77d7e，用户报告）：button（inline-block）继承行高 2.2 后 border-b 画在盒底，低于文字基线显得"掉下来"；改用原生 a 同机制的 text-decoration（decoration-dashed + underline-offset 3px）永远紧贴文字。**教训：行内文字的下划线永远用 text-decoration 而非 border——inline-block/行高继承会让 border 位置漂移**。
- 精选摘抄贴纸卡（commit 221f6c2，用户需求）：详情页「站长荐语」后新增「精选摘抄」版块——Book 可选字段 `excerpts?: string[]`（8 本 fixture 已填公知名句，无摘抄不渲染）；贴纸卡对齐 about 便签语言（实线边框/硬投影/右下折角/复用 nb-tape 胶带/交替微旋转 nth-child 4n），卡色 6 色轮盘（便签黄/抹茶绿/雾蓝/陶粉/米麻/薄荷青，nth-child 6n 循环）；楷体文字+右对齐出处，hover 转正，双列 grid 移动单列。注意：**prettier 格式化 .astro 模板表达式后渲染输出带首尾空白**（`{book.title}` 变 `\n  书名 \n`），文本精确断言必须 trim。
- 摘抄应用手写字体（commit 44f8151，用户需求）：详情页 head 追加 ZeoSeven link（仅详情页，不进共享 SiteHead），books.css 增 `--nb-font-hand: "平方时光体", KaiTi 栈`。**经验：fonts.check() 为 false 只说明 webfont 未注册（CDN 挂），不代表渲染失败——系统已装字体经 font-family 直接命中（local 优先设计），看截图才是最终判定**。
